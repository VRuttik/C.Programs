#include<stdio.h>
#include<string.h>
int main(){

   char string[100], i = 0, length;

   printf("\n Enter the string: ");
   scanf("%s", &string[i]);

   while (string[i] != '\0')
   i++;
   length = i;

   printf("\n The length of the string is: %d", length);

 return 0;

}
