#include<stdio.h>

float area(float r);

int main(){
    float radius,result;

    printf("Enter the radius of the circle: ");
    scanf("%f", &radius);

    result=area(radius);

    printf("Area of the Circle is: %f", result);

}

float area(float r){

return (3.142*r*r);
}
